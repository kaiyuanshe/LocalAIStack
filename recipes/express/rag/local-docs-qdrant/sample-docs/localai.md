# LocalAIStack RAG Smoke Document

LocalAIStack Express RAG indexes local documents and answers questions with citations.

The MVP supports Markdown, TXT, and basic PDF text extraction. It uses a local JSON index first, while Qdrant remains the target vector store for the next backend iteration.

